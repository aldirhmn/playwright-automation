const config = {
  baseUrl: process.env.BASE_URL || "https://www.saucedemo.com",
  baseUrlInquiry: process.env.BASE_URL || "https://demo.midtrans.com/",
  baseUrlPayment:
    process.env.BASE_URL || "https://simulator.sandbox.midtrans.com/",
  // ... other configurations
  // ... other configurations
  // ... other configurations
};

export default config;
